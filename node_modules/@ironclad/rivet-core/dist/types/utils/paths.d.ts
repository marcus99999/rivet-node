interface TreeNode {
    path: string;
    name: string;
    isDirectory: boolean;
    children: TreeNode[];
}
export declare function createTreeFromPaths(paths: string[], rootPath: string): TreeNode;
export {};
